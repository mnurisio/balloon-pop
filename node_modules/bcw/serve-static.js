#!/usr/bin/env node
var http = require("http");
const { handlePortFlags, handleServerRequests, startServer } = require("./lib/server_utils.js");
var port = handlePortFlags()
let server = http.createServer(handleServerRequests);

startServer(server, port);
